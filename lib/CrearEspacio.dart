import 'package:flutter/material.dart';

class CrearEspacio extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Crear Espacio")),
      body: Center(child: Text("Pantalla para Crear Espacio")),
    );
  }
}