import 'package:flutter/material.dart';

class CrearUsuario extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Crear Usuario")),
      body: Center(child: Text("Pantalla para Crear Usuario")),
    );
  }
}