import 'package:flutter/material.dart';

class ImportarHorarios extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Importar Horarios")),
      body: Center(child: Text("Aquí puedes importar horarios")),
    );
  }
}