import 'package:flutter/material.dart';

class ListaEspacios extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lista Espacios")),
      body: Center(child: Text("Lista de Espacios")),
    );
  }
}