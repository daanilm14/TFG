import 'package:flutter/material.dart';

class ListaPeticiones extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("lista de peticiones")),
      body: Center(child: Text("Lista de Peticiones")),
    );
  }
}