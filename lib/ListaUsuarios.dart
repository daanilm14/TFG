import 'package:flutter/material.dart';

class ListaUsuarios extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: Text("Lista de Usuarios")),
      body: Center(child: Text("Lista de Usuarios")),
    );
  }
}